# VendorMind AI — Python Dependencies

# Vector DB & Embeddings
qdrant-client>=1.9.0
sentence-transformers>=3.0.1

# LangGraph (Orchestration)
langgraph>=0.2.14
langchain-core>=0.3.0

# LLM (Gemini — Google AI Studio / Vertex AI)
google-generativeai>=0.7.2

# Backend API
fastapi>=0.115.0
uvicorn[standard]>=0.30.6
pydantic>=2.9.0

# Frontend — pinned exact version to avoid pulling a broken latest build
streamlit==1.39.0
pandas>=2.2.3

# Guardrails (optional — used if ENKRYPT_API_KEY is set)
requests>=2.32.3

# Utilities
python-dotenv>=1.0.1
